# MoA手動オーケストレーション — 小説推敲（整合性検証 + 読者視点評価）

## なぜ手動オーケストレーションか

Hermes Agent の組み込み `moa` ツールセット（`mixture_of_agents`）は以下の制約があるため、小説レビュー用途には手動オーケストレーションを推奨する：

| 項目 | 組み込み moa | 手動オーケ |
|--------|-------------|-----------|
| プロバイダ | **OpenRouter 固定**（`OPENROUTER_API_KEY`必須） | 任意（ユーザの契約プロバイダ） |
| デフォルトモデル | 汎用推論向け（claude-opus, gemini, gpt, deepseek） | 任意に指定可能 |
| モデル指定 | コード内ハードコード（上書きは可能） | CLI フラグで自由 |
| スキル配布適合性 | ❌（プロバイダ依存） | ✅（ユーザ環境に適応） |

---

## 推奨プロバイダー（2026年5月時点）

モデル名は変遷が激しいため推奨しません。プロバイダ選定基準のみを示します。
実運用の際は、現在日付を勘案し `hermes model` で最新のプロバイダ一覧と利用可能モデルを確認してください。

### 選定基準

| 基準 | 説明 |
|------|------|
| マルチモデル | 1 プロバイダで複数モデルを利用可能（MoA 4 エージェントの異モデル割当に有利） |
| フラッグシップ対応 | GPT-5 / Claude-4 / Gemini-3 等の最上位モデルが利用可能 |
| Hermes 親和性 | 公式対応または動作実績があり、認証が安定している |
| 無料枠 | 無料モデルまたは期間限定無料枠がある（試行コスト低減） |
| 料金 | 有料でも低価格帯（$10〜20/月程度の定額または従量課金で手頃） |

### おすすめ（コア）

| プロバイダ | 特徴 |
|-----------|------|
| **OpenCode Go** | 月 $10 定額。複数オープンモデル利用可能。モデル名指定がシンプル（プロバイダプレフィックス不要） |
| **OpenRouter** | 100+ モデル。従量課金。全主要モデルにアクセス可能。MoA のモデル選択肢が最大 |
| **Nous Portal** | Nous Research 公式。フラッグシップモデル利用可能。Hermes Agent との親和性が最も高い |

### 追加候補

| プロバイダ | 特徴 |
|-----------|------|
| **NovitaAI** | GPU Cloud + Model API。エージェントサンドボックスあり。無料枠あり |
| **NVIDIA NIM** | Nemotron モデル。build.nvidia.com またはローカル NIM。無料枠あり |
| **Ollama Cloud** | クラウドホストのオープンモデル。ollama.com 経由。シンプルな従量課金 |
| **OpenCode Zen** | 35+ モデルを従量課金で提供。コスト重視の選択肢 |

### 全プロバイダ一覧の確認

```bash
hermes model
```

2026年5月時点で 30 以上のプロバイダが利用可能。上記以外にも状況次第で有力な選択肢がありうるため、実行時に最新情報を確認する。

---

## 基本コマンド

```bash
hermes chat -q "プロンプト" -m "モデル名" --provider プロバイダ名
```

### 例

```bash
# opencode-go の mimo-v2.5-pro で論理レビュー
hermes chat -q "全4章の論理整合性をチェックして" -m "mimo-v2.5-pro" --provider opencode-go

# nous の deepseek-v4-flash で読者体験レビュー
hermes chat -q "読者視点で評価して" -m "deepseek/deepseek-v4-flash" --provider nous
```

## 4エージェント構成

各エージェントに**異なるLLM**を割り当てること（同一モデルでの複数視点はMoAの意味を損なう）。

| # | 視点 | 役割 | チェック内容 |
|---|------|------|-------------|
| 1 | **論理整合性** | プロット・伏線・キャラ行動の矛盾検出 | 時間軸、設定数値、因果関係、未回収伏線 |
| 2 | **文体・表現技法** | 比喩・五感・リズム・文体一貫性 | クリシェ、五感充足、説明的モノローグ、文長バランス |
| 3 | **時代考証・語彙** | 大正〜昭和初期の語彙・風俗検証 | 外来語、戦後俗語、度量衡、学術用語 |
| 4 | **読者視点評価** | 没入感・感情曲線・余韻評価 | 説明不足/過剰、共感阻害、幕切れ強度、希望/喪失バランス |

## エージェント指示テンプレート

### 必須要件

全エージェントの指示には以下を**必須**で含める：

1. **改善案の明示** — 問題指摘だけでなく、修正後の具体的な文章例を必ず添える
2. **場所の明示** — 章・節・行番号（該当テキスト引用付き）
3. **優先度付与** — 高/中/低

### テンプレート例（論理整合性）

```
以下の日本語小説の全章を精査し、論理整合性の問題を指摘せよ。
各指摘には**必ず改善案（修正後の具体的文章）を添える**こと。

ファイル:
- /path/to/novel/序章.md
- /path/to/novel/第1章.md
（...）

チェック項目:
1. 時間軸の矛盾
2. 設定数値の不整合
3. 未回収の伏線
4. キャラ行動の不自然さ

形式: 【章・節】【行】【問題点】【改善案（具体的文章）】【優先度】
```

## 並列実行

3プロセスまで同時起動可能。`background=true` + `notify_on_complete=true` 推奨。

```bash
# プロセス1
terminal(command="hermes chat -q ... -m MODEL1 --provider P1", background=true, notify_on_complete=true)
# プロセス2
terminal(command="hermes chat -q ... -m MODEL2 --provider P2", background=true, notify_on_complete=true)
# プロセス3
terminal(command="hermes chat -q ... -m MODEL3 --provider P3", background=true, notify_on_complete=true)
```

## 合議集計

全エージェント完了後：

1. 4エージェントの全指摘を統合
2. 複数モデルが共通して指摘した項目を最優先
3. 単独モデルの独自発見も重要度に応じて採用
4. **改善案を横断比較**し、最も効果的な案を選定または合成
5. クロスモデル分析表を作成（どのモデルが何を指摘したか）
6. **【必須】集約した改善案の設定矛盾検出** — 改善案が作品の既存設定（年表、尾の本数と年齢の関係、キャラクターの過去、世界観ルール）と矛盾していないか照合する。LLMは設定の全体像を把握していないため、改善案に「500年前に9尾だった」といった設定違反が混入しうる。矛盾が検出された場合、改善案を修正するか、矛盾のない代替案をユーザに提示する
7. 最終修正リストをユーザに提示し、承認を得てから本文修正に着手する

**実例**: 名前の由来改善案に「九つの尾を持ちながら」→ 設定では50年/1尾で500年前の若い妖狐は2〜4尾。矛盾検出により「いつか九つの尾を揃える日を夢見る」に修正。

## 例外処理

MoA実行中にLLMが以下の状態になった場合：

| 現象 | 対応 |
|------|------|
| タイムアウト（600s） | ユーザに「どのモデル・視点・現象」を報告。代替モデル指定 or スコープ縮小を仰ぐ |
| モデル名エラー（404等） | ユーザに正しいモデル名の確認を依頼 |
| 調査拒否・空応答 | ユーザに報告。別モデルで再実行 |

**禁止事項**: 自動リトライ、ユーザに黙ったままの代替モデル選択、エラーを無視した集計。

## モデル名の注意点

プロバイダごとにモデル名の表記が異なるため、事前確認が必須：

| プロバイダ | モデル指定形式 | 例 |
|------------|-------------|-----|
| opencode-go | `モデル名`（そのまま） | `mimo-v2.5-pro` |
| nous | `プロバイダ/モデル名` | `deepseek/deepseek-v4-flash` |
| openrouter | `プロバイダ/モデル名` | `anthropic/claude-sonnet-4` |

**404エラーが出た場合、モデル名の形式が誤っている可能性が高い。**
